[build-system]
requires = [
    "setuptools>=42",
    "wheel",
    "pybind11>=2.12",
]
build-backend = "setuptools.build_meta"

[tool.cibuildwheel]
test-requires=[
    "pytest"
]
test-command="pytest {package}/tests"
